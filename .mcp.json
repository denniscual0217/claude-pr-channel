{
  "mcpServers": {
    "pr-channel": {
      "command": "sh",
      "args": [
        "-c",
        "export PATH=\"$HOME/.bun/bin:$BUN_INSTALL/bin:/usr/local/bin:$PATH\"; command -v bun >/dev/null || { echo \"pr-channel: bun is not installed or not on PATH\" >&2; exit 1; }; cd \"${CLAUDE_PLUGIN_ROOT}\" && bun install --no-summary 1>&2 && exec bun server.ts"
      ]
    }
  }
}
