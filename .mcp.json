{
  "mcpServers": {
    "pr-channel": {
      "command": "sh",
      "args": [
        "-c",
        "cd \"${CLAUDE_PLUGIN_ROOT}\" && bun install --no-summary 1>&2 && exec bun server.ts"
      ]
    }
  }
}
