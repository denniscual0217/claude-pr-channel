{
  "name": "pr-channel",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Per-session GitHub PR event channel for Claude Code",
  "scripts": {
    "start": "bun install --frozen-lockfile --no-summary 1>&2 && bun server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "schema": "bun scripts/write-schema.ts",
    "config": "bun ui/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "bun-types": "1.4.2",
    "typescript": "7.0.2"
  }
}
