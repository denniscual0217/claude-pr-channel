{
  "name": "pr-channel",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Per-session GitHub PR event channel for Claude Code",
  "scripts": {
    "start": "bun install --no-summary 1>&2 && bun server.ts",
    "test": "bun test",
    "schema": "bun scripts/write-schema.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "bun-types": "1.4.2"
  }
}
